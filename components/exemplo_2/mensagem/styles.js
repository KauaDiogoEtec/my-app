import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    viewMessage:{
        borderWidth: 2,
        borderColor: 'orange',
        borderRadius: 20,
    },
    message:{
        fontSize: 25,
        margin: 10,
        color: 'orange',
    }
});

export default styles;